x-app:
  name: kolibri
  title: Kolibri
  description: |
    Kolibri is an adaptable set of open solutions specially developed to support learning for the half of the world without Internet access. 
    Centered around an offline-first learning platform that runs on a variety of low-cost and legacy devices, the Kolibri Product Ecosystem includes a curricular tool, a library of open educational resources, and a toolkit of resources to support training and implementation in formal, informal, and non-formal learning environments.
  url: https://learningequality.org/kolibri/
  category: education
  icon: icon.png
  author: Koen Swings  # Who created this compose file? (not the app ;-)
services:
  kolibri:
    #image: koenswings/kolibri:1.0-0.15.5-dev
    image: koenswings/kolibri:latest_dev
    init: true
    restart: no
    pull_policy: never
    volumes:
      - ./data:/docker/mnt
    ports:
      - ${port}:8000
      - 3000:3000
    environment:
      - KOLIBRI_RUN_MODE=docker
      # - KOLIBRI_PEX_URL=https://learningequality.org/r/kolibri-pex-latest
      # - KOLIBRI_PEX_URL=default
      # - DOCKERMNT_PEX_PATH=./kolibri-0.11.0a1.dev_git-37-gb15b923.pex
      # - CHANNELS_TO_IMPORT=  # comma-separated list of channel IDs (not set by default)
      # - KOLIBRI_LANG=en # default `en`
      - KOLIBRI_PROVISIONDEVICE_FACILITY=Powerberries
      - KOLIBRI_PROVISIONDEVICE_PRESET=nonformal # default `formal` other options being `nonformal` and `informal`
      - KOLIBRI_PROVISIONDEVICE_SUPERUSERNAME=admin
      - KOLIBRI_PROVISIONDEVICE_SUPERUSERPASSWORD=admin911!
      # Generated by AI
      # - KOLIBRI_LISTEN_PORT=8000
      # - KOLIBRI_LISTEN_ADDRESS=
      # - KOLIBRI_MEDIA_URL=http://localhost:8000
      # - KOLIBRI_ALLOW_HOST=*
      # - KOLIBRI_DEBUG=1
      # - KOLIBRI_LOG_LEVEL=DEBUG
      # - KOLIBRI_HTTP_PORT=8000
      # - KOLIBRI_HTTPS_PORT=8001
      # - KOLIBRI_HTTPS=0
      # - KOLIBRI_HTTPS_CERTIFICATE=
      # - KOLIBRI_HTTPS_KEY=
      # - KOLIBRI_HTTPS_CA=


